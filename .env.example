# Frontend（纯前端模式）
# DeepSeek API key（可选，构建时打进产物作为默认值；运行时由用户在设置弹窗中输入）
# NEXT_PUBLIC_DEEPSEEK_API_KEY=sk-xxxx

# Tavily 联网搜索 API key（可选，https://tavily.com 注册，免费 1000 次/月）
# 用于本地知识库命中不足时联网检索补充；不配置则仅用本地知识库
# NEXT_PUBLIC_TAVILY_API_KEY=tvly-xxxx
